class Place {
  constructor(id, title, ownerId, imageUri, address, location) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.ownerId = ownerId;
    this.address = address;
    this.location = location;
  }
}

export default Place;
